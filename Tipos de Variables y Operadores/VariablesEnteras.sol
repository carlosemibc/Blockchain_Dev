//Indicar la versión:
pragma solidity >=0.4.4 <0.7.0;

contract enteros{

    //Variables sin signo
    uint mi_entero;
    uint mi_entero_ini = 3;
    uint cota = 5000;
    
    //Variables enteras sin signo con numero especifico de bits
    uint8 entero8bits;
    uint64 entero64bits = 7000;
    uint16 entero16bits;

    //Variables eneras con signos
    int mi_entero_signo;
    int mi_entero_neg = -32;
    int mi_entero_normal = 65;

    //Variables enteras con signo y numero especifico de bits
    int72 enteroSigno;
    int240 entero240bits = 90000;

    //Variables booleanas
    
    
}